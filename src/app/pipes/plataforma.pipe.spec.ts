import { PlataformaPipe } from './plataforma.pipe';

describe('PlataformaPipe', () => {
  it('create an instance', () => {
    const pipe = new PlataformaPipe();
    expect(pipe).toBeTruthy();
  });
});
