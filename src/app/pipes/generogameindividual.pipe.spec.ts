import { GenerogameindividualPipe } from './generogameindividual.pipe';

describe('GenerogameindividualPipe', () => {
  it('create an instance', () => {
    const pipe = new GenerogameindividualPipe();
    expect(pipe).toBeTruthy();
  });
});
